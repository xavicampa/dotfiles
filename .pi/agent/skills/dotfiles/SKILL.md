---
name: dotfiles
description: appends git-dir and work-tree to the git command to allow me to interact with my dotfiles from any location
---

# dotfiles

Append --git-dir=$HOME/.dotfiles/ and --work-tree=$HOME to the git command whenever theres is mention of dotfiles.
